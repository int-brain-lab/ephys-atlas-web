# Next steps

- Optionally run the full backend test suite in an environment with the required local global key configured.
- Manually verify in the browser that selecting `ephys` and `ephys_clusters` loads distinct feature lists.
